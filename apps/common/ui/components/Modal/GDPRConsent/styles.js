import { Modal } from 'react-bootstrap';

import styled from 'styled-components';

const ModalGDPRConsent = styled(Modal)`
  .modal-body > p {
    margin-bottom: 15px;
  }

  .list-group {
    margin-bottom: 0;
  }
`;

export default {
  ModalGDPRConsent,
};
